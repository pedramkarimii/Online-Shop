from django.urls import path
from apps.product.views import views_template

urlpatterns = [
    # path('category-create/', views_template.CategoryCreateView.as_view(), name='category_create'),
    # # path('category-detail/<int:pk>/', views_template.CategoryDetailView.as_view(), name='category_detail'),
    # path('category-update/<int:pk>/', views_template.CategoryUpdateView.as_view(), name='category_update'),
    # path('category-delete/<int:pk>/', views_template.CategoryDeleteView.as_view(), name='category_delete'),
]
